# Cloudflare 凭证 —— 由 tools/cf.sh 读取（已 gitignore）
#
# 复制这个文件的副本并改名（去掉 .example）后填值：
#   cp tools/.env.cloudflare tools/.env.cloudflare.bak && mv tools/.env.cloudflare.bak tools/.env.cloudflare
# 或者直接在项目根目录建一个 tools/.env.cloudflare

# ── API Token（推荐，不用浏览器登录）──
# 创建：https://dash.cloudflare.com/profile/api-tokens
#       → Create Token → "Edit Cloudflare Workers" 模板 → 一路下一步
# ⚠ 需要的权限：Workers Scripts:Edit、Workers KV:Edit(可选)、D1:Edit、Account Settings:Read
CLOUDFLARE_API_TOKEN=

# ── Account ID ──
# 位置：https://dash.cloudflare.com → 右侧栏最下面「Account ID」
# 或者 Workers & Pages 页面右侧也能看到
CLOUDFLARE_ACCOUNT_ID=
