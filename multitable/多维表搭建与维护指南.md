# RoomSense 多维表 → 看板自动抓取：操作手册

## 0. 先说清楚一件事

`outputs/dashboard.html` 是**纯静态文件**，数据写死在 JS 常量里，**它本身不可能去读在线多维表**。

要"改多维表 → 看板自动变"，必须跑这套架构（`roomsense-cloud/`，本地已跑通）：

```
飞书多维表（你来维护）
      ↓ 每 30 分钟自动同步（或后台手动点一下）
Cloudflare Worker（API，持有密钥）
      ↓
D1 数据库（SQLite，聚合成看板 JSON）
      ↓
看板 HTML 打开时 fetch('/api/dashboard')
```

密钥只存在于 Worker 的 Secrets 里，**前端页面永远拿不到**。

---

## 1. 建表：飞书里就 4 张，一张不多

先给结论，别被下面的细节绕晕：

| 飞书表 | 对应的 Secret | 维护频率 | 必填？ |
|---|---|---|---|
| **A 销售明细** | `FEISHU_TABLE_ID` | **每周** | ✅ 必需，没它整个同步不跑 |
| **B 库存** | `FEISHU_TABLE_ID_INV` | 每周 / 有到货、盘点时 | 可选，不配就跳过 |
| **C 广告投放** | `FEISHU_TABLE_ID_ADS` | **每周** | 可选，不配就没广告数据 |
| **D SKU 主数据** | `FEISHU_TABLE_ID_SKU` | **首次填一次**，之后低频 | 可选，但**强烈建议配**（采购价在里面） |

另外 3 张数据库表**不用你在飞书建**，系统自己管：

| 表 | 谁来写 | 说明 |
|---|---|---|
| `overrides` | 后台 `/admin.html` | 人工矫正值，优先级**高于**自动聚合。见第 4 节渐进接管 |
| `inventory_snapshot` | 导入库存时**自动追加** | 同一天重复导入只覆盖、不攒重复行 |
| `meta` | `wrangler secret` / D1 直接改 | 汇率、履约费率、补货提前期等全局参数 |

### `multitable/` 下 8 个 CSV 分别干嘛

| 文件 | 用途 | 飞书建表？ |
|---|---|---|
| `01_周销售汇总.csv` | 看板数字的快照，导出留档用 | ❌ 看板自动算 |
| `02_SKU周销量.csv` | 同上 | ❌ 看板自动算 |
| `05_平台周销售额.csv` | 同上 | ❌ 看板自动算 |
| `06_销售明细_每周填写.csv` | **表 A 的模板** | ✅ 每周要填 |
| `07_广告投放.csv` | **表 C 的模板** | ✅ 每周要填 |
| `03_库存.csv` | **表 B 的模板** | ✅ |
| `04_SKU主数据.csv` | **表 D 的模板**（10 列全量） | ✅ 首次导入 |
| `08_SKU成本_待填.csv` | **不是第 5 张表**，只是「只填采购价」的窄版 | ❌ 见下 |

> **`08_SKU成本_待填.csv` 到底是什么**
> `04` 有 10 列，但你第一次真正要补的只有 **采购价** 一列。
> 这份窄版 CSV 就是让你只看 4 列、填 1 列，不被其它列干扰。
>
> 填完之后**两条路都行，选一条**：
>
> - **A（推荐）**：把采购价填进飞书「SKU 主数据」表的 `采购价` 列 → 同步后自动生效，
>   以后调价也在这改，一个地方维护
> - **B**：把 `08` 这个 CSV 传到后台 `/admin.html` 导入 → 只更新 `cost` 一列
>
> 两条路走的是同一段代码（`upsertSkuMaster`），结果完全一样。
> 别两边都填 —— 飞书那边是每周覆盖更新的，会盖掉 CSV 导入的值。

**在一个多维表里建 4 张数据表**（名字随便起，记住每张的 table_id）：

### 表 A：销售明细（最重要）

列名**必须完全一致**（Worker 认中文列名）：

`订单日期` | `平台` | `订单号` | `SKU` | `品类` | `销量` | `商品销售额` | `邮费收入` | `币种` | `汇率`

- 订单日期：飞书里用「日期」字段类型即可（Worker 已兼容时间戳/文本/Excel 序列号）
- 平台名要和现有看板一致：`Bunnings` / `Amazon Marketplace` / `Kmart` / `Dropshipzone` 等 12 个
- SKU 用全名：`XFKF-MA-1772-26-Q`（短名 `MA-1772-Q` 也能认，但建议统一全名）
- 金额：纯数字，不要带 `A$`、逗号、空格（Worker 会尽力清理，但别赌）
- `汇率` 列可以**整列留空**，见下一节
- **这 10 列里没有「周次」，也不用加** —— 见下一节

> **口径**：营收(AUD) = (商品销售额 + 邮费收入) × 汇率
> 商品销售额和邮费收入都填**原币种**金额，币种列写 `AUD` 或 `USD`。

### 1.4 周次不用你填，系统自己算

**销售明细表：不要加「周次」列。**

系统从 `订单日期` 直接推算周次（`2026-08-24` → `W35`），你填了日期就够了。
周次锚点是 `meta.week_epoch_date = 2026-05-30`，**一周 = 周六 00:00 ~ 周五 23:59**（你的平台口径）：

| 日期 | 周次 |
|---|---|
| 2026-08-21（周五） | W34 |
| 2026-08-22（周六） | W35 |
| 2026-08-28（周五） | W35 |
| 2026-08-29（周六） | W36 |

> ⚠ 如果你**已经在表里加了「周次」列**，系统会以你填的值为准（这是个临时覆盖通道）。
> 填错不会报错，只会悄悄污染周趋势 —— 所以看板顶部会检查一遍：
> 手填周次和日期推算值对不上时**会出告警条**，并告诉你涉及几行。
> 看到那条告警，最省事的处理就是把这列删掉。

**想在飞书里也看到周次？** 加一个**公式字段**就行：

```
"W" & TEXT(23 + ROUNDDOWN(DATEDIF(DATE(2026,5,30), [订单日期], "D") / 7))
```

> 列名**别叫「周次」**，叫 `周次(自动)` 或 `周次显示`。
> Worker 只认「周次」这四个字，换个名字它就不会把公式结果当成你的手填值去覆盖 ——
> 纯展示、纯分组筛选用，永远不会跟系统算的打架。
>
> 建完用 `2026-08-22` 验一下，应该显示 `W35`；`2026-08-29` 显示 `W36`。
> （这条公式我没法在飞书里实跑验证，你建好先对这两个日期。）

**「输入 SKU 后自动关联到其他表」—— 系统侧已经做了，飞书里不用建关联字段。**

Worker 按 SKU **文本**去 SKU 主数据表里 join，能自动带出：品类、采购价、售价、履约费率、
补货提前期。所以：

- **「品类」列可以整个不填** —— 系统从 SKU 主数据表取（填了就以你填的为准）
- 采购价、履约成本等**只在 SKU 主数据表维护一次**，销售明细里一行都不用重复填

> ⚠ **SKU 这一列的类型别选「关联」**
> 飞书的「双向关联」字段，API 返回的是 `[{record_id: "rec..."}]`，**里面没有文本**。
> 我的取值逻辑会把这种结构拍成空字符串 → **SKU 变成空，整行直接报废**。
>
> 想要下拉选择的效果，用「**单选**」类型，把 16 个 SKU 建成选项即可 ——
> 既是下拉，API 返回的又是纯文本。

**「销售额关联 SKU」也不需要周次。**
这两个是独立维度：销售明细每一行本来就带 `SKU` 列，系统按 SKU 直接聚合营收和销量；
周次只是把同一 SKU 的数据再按周切开。不填周次不影响 SKU 维度的任何统计。

**广告投放表：可以填「日期」让系统自动算周次**，见 1.5 节下方表 C 的说明。

---

## 1.5 USD → AUD 折算

你的结算单里有 AUD 和 USD 两种（截图里看 Temu - AU 是 AUD，Temu - US 是 USD），
系统只识别这两种币种。

### 三种填法，按优先级

| 你填了什么 | 系统怎么算 |
|---|---|
| ① 填了 `商品销售额` 和/或 `邮费收入` | `(商品销售额 + 邮费收入) × 汇率` ← **推荐，就用这个** |
| ② 只填了 `销售额`，币种是 USD | `销售额 × 汇率` |
| ③ 只填了 `销售额`，币种是 AUD 或留空 | 原值直接当 AUD，不折算 ← **旧格式，完全兼容** |

也就是说：**你原来的表（只有「销售额」一列）不用改也能继续用**，
只是那样没法把商品和邮费拆开统计。想拆就加两列。

### 汇率怎么来（优先级从高到低）

1. **行内「汇率」列**（结算单带的 Order Rate to AUD）—— **你的结算单每行都有，最准**
2. **后台 meta.fx_usd_aud** —— 兜底，万一某行漏填了行内汇率才用

后台加兜底汇率：

```sql
INSERT OR REPLACE INTO meta(key, value) VALUES('fx_usd_aud', '1.50');
```

### 三个防坑设计

- **AUD 永远不会被折算**。就算你行内「汇率」列填了 99，澳元行也不会被乘。
- **不是 USD 就报错**。如果哪天出现 EUR/GBP 行（截图里没有），营收记 0 + 看板顶部出告警条。
- **USD 缺汇率营收记 0，不会静默当 1 折算**。
  宁可让你看见数字掉了，也不要给你一个看起来正常其实是错的营收。

> 第一次配置完后，看一眼 `/api/dashboard` 返回的 `warnings` 是不是空数组。
> 有告警就说明有行没正确配。

### 表 B：库存

`快照日期` | `SKU` | `品类` | `现有库存` | `在途` | `安全库存` | `预计到货`

- **快照日期留空 = 用导入当天**，想补录历史就填 `YYYY-MM-DD`
- 每次导入都会往历史表追加一条，**同一天重复导入只覆盖、不会攒重复行**
- 频率建议每周一次（跟填销售明细同一天），按天导也没人拦你，只是没必要

> ⚠ **快照日期的两个坑**
> 1. 系统只取**第一行**的快照日期给整批用。一次导入别混不同日期的行，
>    要补录历史就**一次一个日期**。
> 2. 飞书里用「日期」字段类型没问题 —— Worker 会把飞书返回的毫秒时间戳转成 `YYYY-MM-DD`。
>    但如果这一列在飞书表里**不存在**，快照日期恒等于同步当天，历史补录就失效了。
>    所以这张表**一定要建 `快照日期` 这一列**。

### 表 C：广告投放

`日期` | `周次` | `平台` | `广告活动` | `花费` | `广告销售额` | `订单数`

**`日期` 和 `周次` 二选一，推荐填 `日期`** —— 系统自动算周次，省得你去对 W 号。

- 填**该周的任意一天**都行：一周是周六到周五，这 7 天里任何一天都会归到同一个 W 号
- 两个都填 → 以 `周次` 为准
- 两个都空 → **这行广告会被整行跳过，不参与毛利和 ACOS 计算**，看板顶部会告警

> 广告必须靠周次才能归到月份（毛利表是「8 月」这种自然月口径），
> 所以周次拿不到，那笔花费就等于白填 —— 这条告警别忽略。

其他注意点：

- 广告活动名建议带上 SKU，例如 `XFKF-MA-1666-34 (Auto)`，
  这样系统能把花费精确归到那个 SKU；对不上的会按营收占比摊
- 命名里的 `(Auto)` / `(Manual)` / `(Brand)` 后缀随便加，系统匹配时会截掉空格后面的部分
- 平台后台导出的英文表头（`Date` / `Campaign` / `Spend` / `Ad Sales` / `Orders`）也认

### 表 D：SKU 主数据

`SKU` | `品名` | `品类` | `规格` | `采购价` | `币种` | `售价AUD` | `履约费率` | `单件履约费` | `补货提前期` | `安全库存天数`

- **`采购价` 是唯一现在就得填的列** —— 不填「8月毛利」那块算不出来，
  看板顶部会一直挂告警条。其余列留 0 系统会自动算或套默认值
- `采购价` 填 RMB 数字（币种列写 `RMB`），系统按 `fx_aud_cny`（当前 4.70）折成 AUD
- `补货提前期`：下单到入仓的天数，留 0 用全局默认 45 天（海运）
- `售价AUD`：留 0 就用月营收/月销量反推的 ASP
- `履约费率` / `单件履约费`：见下一节 —— **这两个是分开的，别混着填**

### 1.9 哪些成本该算进单件毛利（哪些不该）

你列的 11 项成本，按「怎么随销量变化」分成四类，**处理方式完全不同**：

| 成本项 | 归类 | 填到哪 | 为什么 |
|---|---|---|---|
| 销售佣金 | 按售价 % | `履约费率` | 随售价等比变，用 % 才准 |
| 平台佣金 | 按售价 % | `履约费率` | 同上 |
| 退款及退货损耗 | 按售价 % | `履约费率` | 按销售额的一个比率，合并进 % 即可 |
| 快递费（尾程） | **按件 AUD** | `单件履约费` | 按件/按重量收，**不是按售价** |
| 仓库出库处理费 | **按件 AUD** | `单件履约费` | 每出一单收一次 |
| 仓库入库处理费 | **按件 AUD** | `单件履约费` | 按进货量摊到单件 |
| 入仓运费（头程） | **按件 AUD** | `单件履约费` | 按采购批量摊到单件 |
| 仓库卸货费 | **按件 AUD** | `单件履约费` | 同上，按柜/按批摊 |
| 广告费用 | **单独算** | 广告投放表（表 C） | ⚠ **别填进履约成本**，见下方红字 |
| 仓储费 | **不进单件毛利** | 暂不填 | 见下方说明 |
| 视频盘点费 | **不进单件毛利** | 暂不填 | 见下方说明 |

**为什么「按售价%」和「按件」必须分开**（这是这套模型最关键的一点）：

快递和处理费是**按件/按重量**收的，跟售价不成比例。举个真实例子：

| | 售价 | 快递+处理 | 实际占售价 |
|---|---|---|---|
| 床垫 | A$419 | A$45 | **10.7%** |
| 枕头 | A$38 | A$12 | **31.6%** |

**用一刀切的 30%：床垫成本被高估（10.7% → 30%），枕头被低估（31.6% → 30%）。**
结果是你以为床垫不赚钱、枕头很赚钱，**加投决策会做反** —— 而且偏差方向是系统性的：
低单价小件的真实履约费率远高过高单价大件，一刀切永远在骗你。

> ⚠⚠ **广告费千万不要填进履约成本**
> 系统用「**安全垫 = 毛利率 − ACOS**」判断投流值不值，广告费已经在 ACOS 里扣了一次。
> 再塞进履约成本就是**同一笔钱扣两次**：先扣一次算毛利、再扣一次算 ACOS，
> 安全垫被人为压低 → **所有产品都会显示「投流亏损」，你会全面减投**。这是最贵的错法。

**仓储费和视频盘点费为什么不进单件毛利：**

- **仓储费**：按「体积 × 在库天数」收，**卖不卖都在收**。它是持有成本，跟单件毛利无关，
  反映的是**周转率**问题（卖得慢的 SKU 仓储费高）。塞进「售价%」会完全失真。
- **视频盘点费**：按次/按月收的**固定成本**，跟卖几件无关。

这两项该进的是**月度损益**（贡献毛利 = 单件毛利 × 销量 − 仓储费 − 盘点费），
不是单件毛利。等你想要月度损益那张表了告诉我，我加一块。

**现在怎么填最省事**：先只填 `采购价` + 一个 `单件履约费`（把快递和处理费估个数），
`履约费率` 用平台的佣金比例（比如 15%）。剩下的等数据齐了再细化 ——
**缺哪列系统都不会崩，只会显示「待填」并告警。**

> 初始化：把 `03` / `04` 两个 CSV 导入飞书，A 和 C 用空表开始每周填。
> 列名建议直接复制 CSV 的表头，避免手打错字。
> `08` 那份是给你填采购价用的草稿纸，别单独建表。

### 1.6 毛利怎么算的（以及哪些数是估的）

单位经济模型（每件，AUD，不含税）：

```
ASP       = 该月营收 ÷ 该月销量
采购成本  = sku_master.cost ÷ 汇率（RMB 计价时）
佣金+履约 = ASP × fulfil_pct
单件毛利  = ASP − 采购成本 − 佣金+履约
毛利率    = 单件毛利 ÷ ASP
```

**投流判据（这是关键）：**

```
安全垫 = 毛利率 − ACOS
安全垫 > 0  →  投流在边际上赚钱，越大越该加投
```

只看 ACOS 会得出相反结论：毛利率 15% 的产品 ACOS 10% 只剩 5%，
不如毛利率 40% 的产品 ACOS 20%（剩 20%）。

| 安全垫 | 决策 |
|---|---|
| ≥ 15 个百分点 | 厚利·主力加投 |
| 5 ~ 15 | 稳赚·可加投 |
| 0 ~ 5 | 薄利·维持 |
| < 0 | 投流亏损·减投 |

**哪些数是估的（都标了「待核验」）：**

| 参数 | 当前值 | 出处 | 怎么改成真值 |
|---|---|---|---|
| `fulfil_pct`（佣金+履约费率） | 30% | 澳洲大件家居行业经验值，**不是你的真实费率** | 填进 meta，再把 `fulfil_pct_confirmed` 设成 1 |
| `lead_time_days`（补货提前期） | 45 天 | 海运中国→澳洲经验值 | 按 SKU 填进表 D，或改 meta 全局值 |
| `fx_aud_cny` | 4.70 | 你之前给过 | 直接改 meta |
| 采购成本 | **全空** | — | 填飞书表 D 的 `采购价` 列（或导 `08_SKU成本_待填.csv`） |

这些估值在页面顶部的告警条里会一直显形，不会悄悄当成真账用。

### 1.7 安全库存：自动算，你可以覆盖

```
安全库存 SS = 日均销量 × 缓冲天数(14)      ← 纯波动缓冲
目标库存     = 日均 × (提前期 + 缓冲) + SS
建议补货     = max(0, 目标 − 现有 − 在途)
```

- 表 B 里 `安全库存` **填了就用你填的**，填 0 就自动算
- 分级阈值也改成看提前期了：`可售天 < 提前期` 才算「紧急」（现在下单都来不及），
  以前是拍死的 21 天，对海运补货来说太短
- **日均销量取近 28 天**，不是近 7 天 —— 7 天窗口下一周只卖 1~2 件的 SKU
  日均会在 0.14 和 0.43 之间乱跳，可售天跟着从 50 天变 16 天，这就是「数据对不上」的来源

### 手动建表（不依赖我，5 分钟搞定）

不想等 MCP 配好，或者就想自己掌控，按下面做：

1. 飞书 → 云文档 → 新建「多维表」，起个名（比如 `RoomSense 运营数据`）
2. 左下角「+」新建数据表，一共建 4 张，分别改名：
   `销售明细` / `库存` / `广告投放` / `SKU主数据`
3. 每张表的第一行就是表头。**逐个手动改列名** —— 多维表默认第一列叫「文本」且不可删，
   把它改名成第一个列名即可，然后右侧「+」加剩下的列
4. 列类型建议：

   | 列 | 类型 | 说明 |
   |---|---|---|
   | 订单日期 | 日期 | 时间戳/文本/Excel 序列号也认，但日期类型最省事 |
   | 销量、商品销售额、邮费收入、汇率 | 数字 | 汇率留空即可 |
   | 币种 | 单选 | 选项只建两个：`AUD`、`USD` |
   | 平台、订单号、SKU、品类 | 文本 | — |

   > 类型选错不影响同步 —— Worker 读的是**值**，不是类型。
   > 但数字列用数字类型，后面排序和筛选会舒服很多。

5. 建完切到每张表，从 URL 里抠 `table=tblXXXXXXXX`，记下来（步骤 3 用）
6. 想省事：直接把 `multitable/` 下对应的 CSV **拖进多维表**，
   飞书会自动建列并识别表头。但**建完记得核对列名**，飞书偶尔会多建一列或改名字

---

## 2. 接线：4 个值

### 步骤 1 — 建飞书自建应用

**这一个应用，两处共用**：既给 Worker 同步数据用，也给我（飞书 MCP）帮你建表导数据用。
建一次就够，别建两个。

1. 打开 [飞书开放平台](https://open.feishu.cn/app) → 「创建企业自建应用」
2. 权限管理里勾选：
   - `bitable:app`（查看、评论、编辑和管理多维表）—— **必需**
   - `drive:file`（云文档文件）—— 想让我**新建**多维表时才需要；如果你自己建好表再加应用为协作者，可以不勾
3. 「凭证与基础信息」页拿到 **App ID** 和 **App Secret**
4. **发布一个版本**（否则权限不生效，这是最常漏的一步）

### 步骤 2 — 把应用加进多维表
打开多维表 → 右上角「···」→「添加文档应用」→ 搜你刚建的应用名 → 给**可编辑**权限。

> 漏了这步会报 `permission denied` / `91402`，是最常见的坑。
> 如果这个多维表是我帮你新建的，应用自动就有权限，这步可以跳过。

### 步骤 2.5 — 把凭证填进 MCP 配置（让我能帮你建表导数据）

配置文件：`~/.workbuddy-ai/mcp.json`，已经写好了，只差两个值：

```json
{
  "mcpServers": {
    "lark-mcp": {
      "command": "npx",
      "args": [
        "-y", "@larksuiteoapi/lark-mcp", "mcp",
        "-a", "REPLACE_WITH_YOUR_APP_ID",
        "-s", "REPLACE_WITH_YOUR_APP_SECRET",
        "-t", "preset.base.default,preset.base.batch"
      ]
    }
  }
}
```

把 `REPLACE_WITH_YOUR_APP_ID` 和 `REPLACE_WITH_YOUR_APP_SECRET` 换成步骤 1 拿到的值，然后：

1. 打开 WorkBuddy 的**连接器管理**页面
2. 右上角「自定义连接器」入口 → 找到 `lark-mcp` → 点**信任**
3. 重启 WorkBuddy

装好后我可以帮你：新建多维表 → 建那 4 张数据表（列名自动设好）→ 把导出的 CSV 批量灌进去，
**你连手工建表都不用做**。

> `-t` 里两个 preset 的分工：`preset.base.default` 负责建表/列字段/读写单条，
> `preset.base.batch` 负责批量灌记录（121 行 SKU 周销量靠它，一次能塞几百行）。
> 两个都要，少一个就没法批量导。

### 步骤 3 — 拿 table_token 和 table_id
多维表 URL 长这样：

```
https://xxx.feishu.cn/base/BascnXXXXXXXX?table=tblYYYYYYYY&view=vewZZZZ
                              └─── table_token ────┘        └── table_id ──┘
```

4 张表各切一次，记下各自的 `tbl...`。

### 步骤 3.5 — 先自检，别急着注入

拿到凭证后先跑自检，**不用先部署到 Cloudflare** 就能验证接没接对：

```bash
node tools/check_feishu.mjs \
  --app-id=cli_xxxxxxxx \
  --app-secret=xxxxxxxx \
  --token=BascnXXXXXXXX
```

它会一次性告诉你：

1. 鉴权通不通（App ID/Secret 对不对）
2. 多维表能不能读到（最常见的失败是**没把应用加为协作者**）
3. **列出所有数据表的 table_id** —— 不用你一个个去 URL 里抠
4. 目标表的列名有没有对上（差一个字都会报出来）
5. 打印一行原始数据，让你看日期是不是时间戳、金额是不是带符号

全绿再往下走。加 `--debug` 看原始 API 返回。

### 步骤 4 — 注入 Secrets（**不要写进任何代码文件**）

密钥只走两条路：环境变量，或 `wrangler secret put`。**脚本里一个密钥都不存**，
所以 `setup_feishu.sh` 可以放心提交到 GitHub。

把值写进 `tools/.env.feishu`（模板见 `tools/.env.feishu.example`，
这个文件已在 `.gitignore` 里，不会进版本库）：

```bash
cp tools/.env.feishu.example tools/.env.feishu
# 然后编辑它，填真实值
```

跑一次注入：

```bash
bash tools/setup_feishu.sh
```

脚本会校验必填项、逐个注入、列出已配置的 Secrets，最后打印 3 条手动命令
（部署 / 触发同步 / 看日志）。

手动注入也行：

```bash
npx wrangler secret put FEISHU_APP_ID        # cli_xxxxxxxx
npx wrangler secret put FEISHU_APP_SECRET
npx wrangler secret put FEISHU_TABLE_TOKEN   # BascnXXXXXXXX
npx wrangler secret put FEISHU_TABLE_ID      # 销售明细表 tbl...
npx wrangler secret put FEISHU_TABLE_ID_INV  # 库存表 tbl...（可选）
npx wrangler secret put FEISHU_TABLE_ID_ADS  # 广告表 tbl...（可选）
npx wrangler secret put FEISHU_TABLE_ID_SKU  # SKU主数据 tbl...（可选）
npx wrangler secret put ADMIN_TOKEN          # 后台写入口令，自己定一个
```

后 3 个是可选的：配了就同步，不配就跳过，互不影响。

---

## 3. 自动抓取怎么发生

- **自动**：`wrangler.toml` 里已配 `crons = ["*/30 * * * *"]`，每 30 分钟同步一次
- **手动**：后台 `/admin.html` 第 ④ 区点「立即同步」，或 `curl -X POST https://<你的域名>/api/sync/feishu -H "X-Admin-Token: <ADMIN_TOKEN>"`
- **看板**：每次打开页面都重新 `fetch('/api/dashboard')`，所以同步完刷新即生效

同步策略（避免重复/丢失）：

| 表 | 策略 |
|---|---|
| 销售明细 | 删掉 `source='feishu'` 的行再全量写入 → **CSV 导入的历史数据不会被误删** |
| 库存 / SKU 主数据 | 按 SKU 覆盖更新 |
| 广告投放 | 全量替换（多维表是唯一来源时最干净） |
| 库存快照 | 每次同步**追加一条当天快照**，同日重复同步只覆盖 |

「覆盖更新」的意思是：多维表里**没有**的 SKU，数据库里的旧记录**不会被删**；
多维表里**有**的 SKU，以多维表为准。所以多维表可以是「只放最近在卖的 16 个 SKU」这种局部表，
不会把库里的历史 SKU 抹掉。

反过来，**广告投放是全量替换** —— 多维表里删掉一行，下次同步后那行广告数据就没了。
想留历史就别从多维表删行，加行就行。

---

## 4. 关键：渐进接管，别一次性切

现在库里的数字是以 **overrides（人工矫正值）** 形式落库的，优先级**高于**自动聚合。
所以你一开始往多维表填明细，看板数字**不会立刻变** —— 这是故意的，防止新旧数据打架。

切换节奏建议（每次切一块，对上了再切下一块）：

1. **先切平台**：填一周明细 → 后台第 ③ 区删掉 `platData` 这个 override → 看平台占比对不对 → 对上了继续
2. **再切周趋势**：删 `weekly.W35`（以及其它 `weekly.W##`）→ 看各周销售额
3. **再切 SKU 趋势**：删 `skuWeek.<SKU>.<W##>`（几十条，可以一次全删）
4. **最后切库存/建议**：删 `invData` / `invSuggestData`
5. **品类**：删 `catData`

overrides 清空后，看板就 100% 由多维表明细驱动。

> 任何一步发现数字对不上，把 override 加回去就行，不会破坏数据。

---

## 5. 每周你要做的事（最终态）

**每周（3 件事，10 分钟内）**

1. 往「销售明细」表贴本周订单（各平台后台导 CSV → 整理成 10 列 → 粘进多维表）
   - 商品销售额 / 邮费收入分开填，币种别漏（澳元行填 `AUD`，美元行填 `USD`）
   - 汇率列留空即可，用后台配的全局 `fx_usd_aud`
2. 往「广告投放」表填本周各平台花费和广告销售额
3. 「库存」表更新现有库存 / 在途（有到货或盘点时改）
   - 补录历史要填 `快照日期`，且**一次只导一个日期**

**不用管**

- 什么都不用点 —— 30 分钟内看板自动更新
- 库存快照自动追加，不用你手动归档

**低频（变了才动）**

- 美元汇率波动大时，更新 meta 里的 `fx_usd_aud`（或按周加 `fx_usd_aud.W35`）
- 采购价变了 → 改「SKU 主数据」表的 `采购价` 列
- 其他 3 张表（overrides / inventory_snapshot / meta）**不在飞书里**，别去找

---

## 6. 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| `未配置飞书 Secrets` | 4 个必需值没配全 | `npx wrangler secret list` 检查 |
| `飞书鉴权失败` | App ID/Secret 错了，或应用没发布版本 | 回开放平台发布版本 |
| `飞书读取失败(permission denied)` | 应用没加进多维表协作者 | 步骤 2 |
| 同步成功但看板没变 | overrides 还在覆盖 | 按第 4 节删对应 override |
| SKU 在库存表里匹配不上 | 短名/全名混用 | 统一用全名 `XFKF-MA-1772-26-Q` |
| 销售额变成 0 | 单元格带了 `A$`/逗号/空格 | 改成纯数字（Worker 会尽力清理，但别赌） |
| 美元行营收变成 0 | 没配 `fx_usd_aud` | 后台 meta 加 `fx_usd_aud`；看 `/api/dashboard` 的 `warnings` |
| 美元营收明显偏大/偏小 | 用了过期的全局汇率 | 按周加 `fx_usd_aud.W35`，或在行内 `汇率` 列直接填 |
| 澳元行也被乘了汇率 | 不会（AUD 恒为 1），若出现请报 bug | — |
| 广告花费没算进 ACOS | 那行没周次也没日期，被整行跳过了 | 补「日期」或「周次」列 |
| 告警说「N 行销售明细周次对不上」 | 表里加了「周次」列且和订单日期矛盾 | 删掉「周次」列，让系统从日期算 |
| CSV 导入后中文列名对不上 | 文件没带 UTF-8 BOM | 用 `tools/export_for_multitable.mjs` 重新导出（已自动加 BOM） |

---

## 7. 部署到 Cloudflare

**先部署，再接线**。没部署的话飞书接了也没地方跑。

### 7.1 授权（两种方式，选一个）

**方式 A：浏览器点一下（推荐，最省事）**

```bash
cd roomsense-cloud
npx wrangler login
```

会自动开浏览器，登录 Cloudflare 后点「Allow」即可。
如果没自动开，终端会打印一个 URL，手动复制到浏览器。

**方式 B：API Token（headless / 服务器用）**

去 Cloudflare Dashboard → My Profile → API Tokens → Create Token，
用「Edit Cloudflare Workers」模板，额外加上 D1 的编辑权限。
然后：

```bash
export CLOUDFLARE_API_TOKEN=xxxxxxxx
```

> **不要给全局管理员 Token**，权限最小化即可。

### 7.2 建库 + 建表 + 灌数据

```bash
npx wrangler d1 create roomsense-db      # 输出里有 database_id
```

把 `database_id` 填进 `wrangler.toml` 的 `database_id = "..."` 处，然后：

```bash
npx wrangler d1 execute roomsense-db --remote --file=schema.sql
npx wrangler d1 execute roomsense-db --remote --file=seed.sql
```

`seed.sql` 会把现有看板数字原样灌进去，所以部署完第一眼看到的跟现在一模一样。

### 7.3 注入 Secrets + 部署

```bash
bash tools/setup_feishu.sh     # 或手动 npx wrangler secret put ...
npx wrangler deploy
```

部署完会给一个地址，例如 `https://roomsense-dashboard.<子域>.workers.dev`：

- `/` → 看板
- `/admin.html` → 数据录入后台
- `/api/health` → 健康检查（先 curl 这个确认活着）

### 7.4 验证同步

```bash
curl -X POST https://<你的域名>/api/sync/feishu -H "X-Admin-Token: <你的口令>"
```

返回 `{"ok":true,"synced":{"sales":N,"inventory":N,"ads":N,"sku":N,...}}` 即通。
